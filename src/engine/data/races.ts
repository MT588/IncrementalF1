/**
 * Races are not built yet — this is the gate the game already points at, so the
 * number lives somewhere the race plan can grow around rather than inline in a
 * component. The field, the payouts and the season structure land here next.
 */

/**
 * Completed laps before races open. Reached in about ten minutes of play: long
 * enough that the whole backyard ladder is bought and understood first, short
 * enough to be visible from the moment race craft puts the panel on screen.
 */
export const RACE_UNLOCK_LAPS = 250;
